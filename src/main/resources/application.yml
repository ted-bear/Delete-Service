spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/deleteservice?currentSchema=deleteservice
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  sql:
    init:
      mode: always
      continue-on-error: false
